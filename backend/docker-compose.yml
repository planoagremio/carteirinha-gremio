services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: gremio
      POSTGRES_PASSWORD: gremio
      POSTGRES_DB: gremio_acesso
    ports:
      - "127.0.0.1:5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
